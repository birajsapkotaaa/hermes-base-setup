Jordan corrected that entrepreneur/project work should use only `jordatech` GitHub repositories, not `startupteams` repos, unless explicitly authorized in the current task.
§
Jordan prefers hardware/server recommendations to be grounded in the actual intended use case, constrained to the stated remaining-parts budget, and include used-market sourcing such as eBay rather than Amazon-only links.